<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="EquineConnect.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:EquineConnect.Mobile"
    xmlns:views="clr-namespace:EquineConnect.Mobile.Views"
    Title="EquineConnect.Mobile"
    FlyoutBehavior="Flyout">

    <ShellContent ContentTemplate="{DataTemplate views:StartPage}"
              Route="StartPage" />

    <ShellContent ContentTemplate="{DataTemplate views:LoginPage}" 
                  Title="Login"
                  IsVisible="{Binding IsLoginPageVisible}"
                  Route="LoginPage"/>


    <TabBar>
        <Tab Title="Aviseringar" Icon="house_solid.svg">
            <ShellContent ContentTemplate="{DataTemplate views:MainPage}"
                          Title="Aviseringar"
                          Route="MainPage"                          />
        </Tab>
        <!--<Tab Title="Kalender" Icon="calendar_days_solid.svg">
            <ShellContent ContentTemplate="{DataTemplate views:CalendarPage}"
                          Title="Kalender"/>
        </Tab>
        <Tab Title="Stallet" Icon="people.svg">
            <ShellContent ContentTemplate="{DataTemplate views:PersonPage}"
                          Title="Person"/>
            <ShellContent ContentTemplate="{DataTemplate views:HorsePage}" 
                          Title="Häst"/>
        </Tab>-->
    </TabBar>

    <!--<FlyoutItem FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent ContentTemplate="{DataTemplate views:ProfilePage}"
                      Title="Profil"
                      Route="AboutPage"/>
        --><!--<ShellContent ContentTemplate="{DataTemplate view:SettingsPage}"
                      Title="Inställning"
                      Icon="gear_solid.scg"/>--><!--
        <ShellContent ContentTemplate="{DataTemplate views:AboutPage}"
                      Title="Om"
                      Route="AboutPage"/>
    </FlyoutItem>-->
    <MenuItem Text="Logga ut"
              IconImageSource="log_out.svg" 
              Command="{Binding LogoutCommand}"/>

</Shell>
